class PlaylistEndpoint {
  var client: HttpClient
  construct (this.client)
  fun getPlaylist(id: string) { return null }
  fun tracks(id: string, options: any) { return null }
  fun create(name: string, options: any) { return null }
  fun update(id: string, options: any) { return null }
  fun save(playlistIds: List) { }
  fun unsave(playlistIds: List) { }
}
